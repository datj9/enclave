.claude
.devkit
.git
.github
.env
.env.local
.env.*.local
# Root-anchored patterns miss nested installs, e.g. packages/*/node_modules.
**/node_modules
.next
coverage
playwright-report
test-results
blob-report
.playwright
dist
*.tsbuildinfo
.DS_Store
*.log
# Not part of the server or the operator toolbox. `next build` type-checks every *.ts under the
# context (tsconfig include is **/*.ts), so leaving these out only narrows that check; CI still
# type-checks the whole tree.
docs
tests
packages
*.md
playwright.config.ts
vitest.config.ts
